# P0-06 Proliferation & Stress Response

## Purpose

Package precomputed whole-product and state-specific program evidence as a
deterministic, stage-aware candidate profile. P0-06 does not read expression
matrices or rerun single-cell analysis.

## Contract

| Field | Value |
|---|---|
| Package version | `0.2.0` |
| Runtime state | `implemented` |
| Scientific state | `candidate` |
| Optional | `no` |
| EnvironmentSpec | `ENV-P0-CORE-v0.1` (`health_check_passed`) |
| Input envelope | `bridge://schemas/tool-request/v0.2` |
| Output envelope | `bridge://schemas/tool-run/v0.2` |
| Result schema | `bridge://schemas/proliferation-stress-response-profile/v0.1` |
| Adapter | `bridge.tool_packages.p0_06_proliferation_stress_response.adapter:adapter` |

The Python SDK uses `ToolRequestV2` through
`ToolRegistry.load_default().check_eligibility(request)` and `.run(request)`.
The CLI equivalents are:

```bash
bridge-tool validate --request /absolute/path/to/p0_06_request.json
bridge-tool run --request /absolute/path/to/p0_06_request.json
```

The committed example is documentation-only and contains placeholder paths and
checksums:
`examples/requests/p0_06_proliferation_stress_response.json`.

## Structured inputs

Exactly seven immutable `application/json` objects are required. Every
`StructuredInputRef` requires an absolute regular-file path, exact lowercase
SHA-256 checksum, Schema URI and matching object version.

| Role | Schema | Main fields and lineage |
|---|---|---|
| `product_case` | `bridge://schemas/product-case/v0.1` | Product definition, assay, source unit and MeasurementSpec refs |
| `product_definition_card` | `bridge://schemas/product-definition-card/v0.1` | Product definition/version and supported assay |
| `development_window_spec` | `bridge://schemas/development-window-spec/v0.1` | Confirmed/unconfirmed state and externally declared applicable stages |
| `program_spec` | `bridge://schemas/program-spec/v0.1` | Program and gene-set refs/checksums, allowed scopes/states/stages/metrics, coverage threshold, LOD states, review mapping and attribution counts |
| `cell_state_evidence_profile` | `bridge://schemas/cell-state-evidence-profile/v0.2` | Upstream state evidence, assay and MeasurementSpec binding |
| `protocol_ir` | `bridge://schemas/protocol-ir/v0.1` | Metadata completeness, batch confounding, independent-replicate/comparison counts and declared process-step IDs |
| `program_evidence_bundle` | `bridge://schemas/program-evidence-bundle/v0.1` | Precomputed records plus IDs and checksums binding all six upstream objects |

The external `ProgramSpec` is authoritative for all program IDs, gene-set
assets, applicable stages/states/scopes, metric IDs, coverage threshold, LOD
rules, review outcomes and follow-up references. None of these biological
choices is encoded in the adapter.

Expression assets, top-level MeasurementSpecs, arbitrary parameters, missing or
duplicate roles, unsupported media, checksum/version/Schema drift, ProductCase
or ProductDefinition mismatch, cell-state MeasurementSpec mismatch, unregistered
aggregation methods and evidence outside the external spec are refused.

## Evaluation and output

P0-06 evaluates each precomputed record in a fixed order:

1. stage and state applicability;
2. gene coverage against the external threshold;
3. LOD against externally allowed/resolvable states;
4. the external evidence-state-to-review-outcome mapping;
5. process-attribution eligibility.

Missing process metadata, unresolved batch confounding or insufficient
replicate/comparable-group counts produce `cannot_attribute`. The count
requirements are external ProgramSpec fields. Low coverage produces
`unavailable`; unresolved LOD produces `cannot_resolve`; neither becomes
zero or a negative biological finding.

The result contains checksum source bindings,
`ProgramEvidenceSummary[]` and aligned `TranscriptomicReviewFlag[]`.
Review states are `transcriptomic_review_flag`,
`not_detected_above_lod`, `cannot_resolve` or `not_assessed`, and every
flag remains `shadow`. An untriggered rule carries the invariant
`not_evidence_of_safety`.

A successful run publishes two checksummed JSON artifacts:

- `proliferation_stress_response_profile.json`;
- `artifact_manifest.json`.

Repeated identical inputs reuse byte-identical output. Input replacement or
existing-bundle drift fails closed with a typed reason code. No visualization
or MeasurementResult is emitted.

## Scientific boundary

The registered implementation methods only identify deterministic sample/state
aggregation and design auditing. They do not claim that the upstream program
scores are scientifically validated.

The profile always remains `descriptive_only`, `candidate/shadow`,
`domain_score=null` and `score_state=unavailable`. A transcriptomic review
flag is a request for orthogonal review, not evidence of tumorigenicity, genomic
abnormality, clinical risk or product failure. P0-06 does not establish safety,
potency, efficacy, GMP release or product rank.

Detailed requirement:
`docs/bridge_spec_v0.1/proliferation_stress_response_task_card.md`.

Validation record:
`docs/validation/p0_06_proliferation_stress_response_20260825.md`.
