# Environment Card: r4.3

| 字段 | 内容 |
| --- | --- |
| Environment ID | `ENV-R43-v0.1` |
| Environment name | `r4.3` |
| 审计日期 | 2026-08-03 |
| 状态 | `existing_partial` |
| Owner | `pending_assignment` |
| Interpreter | R 4.3.3 |
| GPU requirement | 无；默认 CPU |
| Resource class | `interactive_cpu` / `batch_cpu` |
| Registered tool IDs | `SINGLER`, `EMPTYDROPS`, `SOUPX`, `PSEUDOBULK-DE`, `NICHENET`；均未晋升为已部署正式工具 |
| Input artifact types | h5ad/Matrix Market/parquet/TSV/JSON；RDS 限环境内 |
| Output artifact types | parquet/TSV/JSON/h5ad；RDS 作为附加产物 |
| Update policy | 新 R/Bioconductor snapshot 使用新 Environment ID 并保存 `sessionInfo()` |

## Audited Stack

| Package | Version/状态 |
| --- | --- |
| Seurat | 5.0.3 |
| SingleCellExperiment | 1.24.0 |
| SingleR | 未确认安装 |
| DropletUtils | 未确认安装 |
| SoupX | 未确认安装 |
| edgeR/DESeq2 | 未确认安装 |
| NicheNet/CellChat | 未确认安装 |

## Purpose

承载 R/Bioconductor-only 或 R 优先的方法，包括 SingleR、emptyDrops、SoupX、pseudobulk differential testing 和通讯 sensitivity channels。

## Artifact Contract

- 输入输出优先使用 h5ad、Matrix Market、parquet/TSV 和 JSON manifest。
- Seurat/RDS 可作为环境内 artifact，但跨环境正式交换必须同时导出可验证标准格式。
- 保存 R sessionInfo、Bioconductor version、package versions 和 source snapshot。

## Boundaries

- 当前只确认 R、Seurat 与 SingleCellExperiment；其他候选工具不能描述为已部署。
- Python/R 间 conversion 必须检查 sparse dtype、raw counts、feature IDs、cell IDs 和 categorical metadata。
- 不在现有环境中无审计地批量安装所有候选包。

## Health Check

```bash
conda run -n r4.3 Rscript -e 'sessionInfo()'
conda run -n r4.3 Rscript -e 'stopifnot(requireNamespace("Seurat", quietly=TRUE), requireNamespace("SingleCellExperiment", quietly=TRUE))'
```

每个新增 R 工具应在本卡片或新的专用环境卡中登记，并完成 fixture 与 license audit。
