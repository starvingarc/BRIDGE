# Step 7 Assessment And Evidence Coordination — 2026-09-11

## Research question and observed result

The question is whether a researcher can follow cell-state and product evidence
through an approved, finite check–evidence loop without losing denominators,
uncertainty, missingness or the original source versions.

One genuine GSE204796 D28 process sample was uploaded through the private Web
interface. Experimental facts were reviewed and explicitly confirmed; biological
independence remained unknown. The raw matrix contained 6,247 observations and
33,538 features. It was not declared a final manufactured preparation.

The real run produced:

| Evidence | Observed result | Interpretation boundary |
|---|---|---|
| Default input/count QC | Limited readiness; all 6,247 observations retained | No filtering specification, doublet detection, ambient correction or cell-calling assessment; this is not a QC-filtered or qualified-cell denominator |
| Candidate cell-state support | 4,171/6,247 source-consensus observations and 2,076/6,247 source-conflict observations | Candidate, uncalibrated reference support, not product-role assignment or target purity |
| Conditional finer-state analysis | 4,126 eligible observations, kept as its separate denominator | Eligibility and single-source support do not establish reviewed fine-state identity |
| Exploratory cell cycle | Predicted G1 3,587; S 1,133; G2M 1,527; S/G2M fraction 2,660/6,247 | Descriptive phase prediction, not division rate, quiescence proof, independent replication or a product score |

Cell-state processing reported 24 collapsed duplicate symbols, leaving 33,514
analysis genes. The original matrix remained unchanged. The cell-cycle analysis
used the packaged version-bound Seurat candidate resource and retained
`descriptive_only`, unknown independence and `domain_score=null`.
The other six process families remained separately unavailable; no name-based
mapping was invented to populate them.

The model selected the eligible checks, but no applicable product definition,
measured-domain requirement set or measured graph assertions became available.
Target/regional, developmental, product-role and formal source-bound process
assessment remained blocked. No real graph compilation/query/update or qualified
report was produced. These results establish bounded software connectivity and
candidate observations, not full Step 7 scientific completion.

## Genuine browser acceptance

The observed path was upload → model-assisted fact draft → explicit confirmation
and reviewed correction → finite-scope approval → QC → cell-state evidence →
exploratory cell cycle → rendered values and local evidence drilldown.

The scope allowed at most four tool runs and six model turns. Its first phase
stopped after one tool and two turns: the model incorrectly treated missing
results and another mode's prerequisite as execution blockers. The original
decision and stop were preserved. Registered action purposes and exact-mode
blockers were then clarified at the existing model boundary.

After a controlled temporary-service restart, the same session and scope were
explicitly continued. The model selected cell-state and exploratory cycle checks,
then stopped at `no_discriminating_check` with three tools and five turns used.
No runnable candidate remained. Original authorization, facts, scope digest,
first model turns, stop event and QC receipt were compared against the archived
pre-change session and were unchanged. Counters advanced from 1/2 to 3/5 without
being reset or refunded.

Browser values retained the exact tool counts/fractions and the two distinct
6,247/4,126 denominators. The stopping history and receipt links were visible.
All three receipt hashes and 57 canonical artifact hashes matched; downloaded
QC and cell-state SVGs matched their displayed canonical artifacts.
The original source and uploaded raw matrix hashes were unchanged.
No existing preview service was modified; task-only temporary services were
closed normally and their complete evidence retained privately.

The earlier ordinary-chat `stage_input_construction_failed` had no captured
exception location. It did not recur in the new real fact-drafting path, but
non-reproduction is not a confirmed root-cause fix.

## Measured graph feedback: separate synthetic proof

A retained four-cell synthetic case exercised registered graph query v1 →
source-bound process checking → two-domain sufficiency → canonical append v2 →
query v2. Ten original shadow developmental records in one evidence family,
including 2/4 = 0.5, remained immutable. Five process measurements stayed
null/unavailable; open requirements changed 11 → 12. The final stop was
`no_discriminating_check`, not scientific completion.

This synthetic chain cannot replace the blocked real measured-feedback gate.
An unchanged request or query creates no new biological evidence. No
whole-question completion contract is configured; model-proposed completion
remains `completion_contract_unavailable`.

## Consolidated repair and exact verification scope

The whole-branch review at `ccbe5ed0` found zero Critical, five Important branch
defects, one Important inherited intake blocker and one Minor Schema-parity
defect. One consolidated repair covered hard-count composition, graph-query
semantics, persistent stop history, query-only prerequisite handling, historical
compiler receipts, complete fact editing and query Schema/runtime parity.
The same batch aligned the stale compiler examples and affected tests.

The final action-context supplement was integrated at `0179cf4b`.
Strict request/result validation, original graph bytes, scientific eligibility,
scope authority and budget accounting were retained. Null, withheld text and
numeric projection states remain distinct; existing row/byte bounds still apply.
Privacy assertions cover actual serialized JSON and native-tool payloads with
opaque per-turn aliases, not generic anonymization.

| Check | Actual evidence |
|---|---|
| First integrated installed backend, `ccbe5ed0` | 2,537 passed, 7 failed, 199 warnings, no skips; the seven failures were preserved and addressed, not relabeled a passing run |
| Consolidated repair covering tests | 135 passed / 1 fixture failure; that fixture-only case passed separately; eight projection-boundary cases subsequently passed |
| Action-context regression | Two initial failures; intermediate native-string handling error caused four failures / three passes; corrected seven-case selection passed with two existing warnings |
| Earlier complete frontend, `281cf785` | 108 passed across 12 files |
| Final changed frontend and dependent intake coverage | 27 passed across three files; TypeScript and production build passed; all 46 source files matched the retained build |
| Production asset warning | 581.36-kB main chunk exceeds the unchanged 500-kB warning threshold |
| Final source/wheel preflight, `0179cf4b` | 374 package files byte-matched and all 12 registered implemented tools imported |
| Final installed complete backend, `0179cf4b` | 2,564 passed, one stale stop-reason assertion failed, 199 warnings, no skips in 2,703.51 seconds |
| Final registered tools, knowledge/figures, source policy and branch whitespace | All passed; 12 registered implemented tools imported |
| Stop-history assertion alignment | One test-only correction integrated at `94a7eb6e`: 1 passed, 6 warnings in 8.71 seconds against the same installed wheel; production code remains identical to `0179cf4b` |

The remaining full-run failure expected a changed input to overwrite an original
terminal stop reason. The current contract retains that event and reports input
staleness separately. Its regression is aligned to assert both the unchanged
history and the new `historical`/`input_revision_changed` freshness, with the old
approval still refused and no extra tool or model use. The failed complete run
remains a failed run; the targeted correction is not a second all-green full
baseline.

The complete-backend run uses a clean archived source and its installed wheel,
not a source-path substitute. It includes the configured pinned BPL 2.4.0
compiler. Its reproducible gates are the complete `python -m pytest -q tests`
run, registered-tool discovery, knowledge/figure validation, committed-file
policy and full-branch whitespace checks. Exact commands, source identities,
logs and private data bindings are retained with the run evidence.

Earlier source-specific checks are retained at `ccbe5ed0` in this record's Git
history: query integration (411 passes), scope/coordinator checks (472 unique
passes plus separately resolved compiler cases), producer assembly and the
synthetic feedback chain. Those exact-code results were reused; their
intermediate failures, skips and warnings are not a final all-green baseline.
No dependency upgrades or warning suppression were used. The preinstalled npm
launcher issue was bypassed using its already-installed Node entry points, not
reported as repaired.

## Later integrated mainline and PR review — 2026-09-12

The earlier observations above retain their original source-specific scope.
The later implementation adds native measured evidence, versioned explanations,
same-snapshot research attachments and dependency-scoped corrections. A later
genuine GSE204796 D28 run produced the non-null cycle observations above and
executed a graph query, but did not complete graph append, a valid structured
explanation and the updated report in one model-driven scope. Free-text-only
model output and invalid evidence references are rejected, not converted into
verified scientific explanations. Explicit resume at `0ca2f040` supplies only
allowlisted rejection feedback while preserving used budgets and stop history.

| Later check | Actual evidence |
|---|---|
| Earlier complete backend, `b785f97a` | 2,664 passed / 18 failed / 207 warnings; this remains a failed complete run |
| Consolidated affected installed backend, `5f31856d` | 753 passed / 13 warnings in 2,173.81 seconds; not a second complete-suite baseline |
| Installed resume-feedback regression, `0ca2f040` | 5 passed / 2 warnings in 12.49 seconds |
| Integrated frontend | 116 passed across 13 files; TypeScript and production build passed; the existing 588.83-kB chunk warning remains |
| Source/wheel preflight, `0ca2f040` | 397 package files byte-matched; all 12 registered implemented tools loaded |
| Initial PR #99 CI, run `34593765302` | Backend progress reached 90% without a reported failure before the job's 45-minute timeout; later gates did not run, so this was not a pass |

The merge review checked approval and resume authority, current/historical
evidence binding, correction dependency propagation and canonical report
registration. Further report inspection found one blocking defect: raw historical
`active` flags were used instead of the append-only graph's effective state.
A superseded record could collide with its successor's claim identity, and old
measurements could conflict with current context or reappear in a report summary.
The correction reuses the compiler's existing history validation for draft claims,
exact-draft verification, native context and current HTML summaries. Source
records and downloadable history remain unchanged. Regression cases first failed
on the previous implementation and cover supersession, invalidation, preserved
history and withdrawn native context. The complete installed report module passed
39 tests in 27.08 seconds, including those revision regressions; repository policy
and whitespace checks passed. P0-10 advances from 0.4.4 to 0.4.5 to keep changed
report generation distinct from existing immutable runs. Package examples and
version-bound integration fixtures move together; the final full CI remains required.
The CI time budget is increased to 90 minutes and reports the slowest tests;
the same complete suite and all subsequent gates remain required. The exact final
CI result belongs to [PR #99](https://github.com/starvingarc/BRIDGE/pull/99), not
an anticipated result in this record.

This PR is bounded engineering integration. The genuine continuous browser chain
and same-version report download followed by confirmed correction remain open
in the [mainline plan](../../plans/step7-evidence-coordinator.md), alongside the
separate scientific qualification plans. Neither integration nor passing CI
establishes those acceptance results or authorizes deployment.

## Renewed project and merge review — 2026-09-12

The consolidated review covered execution authorization and version dispatch,
candidate/development constraints, native measurement and graph provenance,
append-only history, report/correction/conditional flows, frontend API and
display boundaries, packaging and maintained documentation. It identified two
remaining engineering blockers initially, with a third confirmed in the legacy
report entry point below; none is a new scientific qualification result:

- The report package advanced to 0.4.5 while the registry's research-result
  dispatch stopped at 0.4.4. GitHub run `34677935760` completed with 2,647 passes,
  three failures, 46 optional-runtime skips and 206 warnings in 3,035.70 seconds.
  Report execution and its downstream feedback chain were refused. The registry
  now admits the current supported research contract and explicitly preserves
  historical 0.4.4/0.4.5 receipts without accepting them as fresh current runs.
- CSV retained source lifecycle flags without exposing effective graph state.
  It now includes `effective_lifecycle_state` and `applicability`, while retaining
  original `lifecycle_state` and `source_json`. The same revision regressions
  cover superseded, invalidated and current values across HTML and CSV.

P0-10 advances to 0.4.6 for the changed export; its package spec, Tool Card,
examples, version-bound fixtures, current dispatch and historical compatibility
are synchronized. Both defects were reproduced before their fixes.

The intermediate installed registry/report/native-feedback selection passed
54 tests with two dependency deprecation warnings in 397.97 seconds. After the
final CSV and version changes, the installed registry and complete report module
passed 52 tests in 30.18 seconds. Frontend verification passed all 116 tests in
13 files, TypeScript and production build; the existing 588.83-kB chunk warning
remains. Repository policy, knowledge/figure registries and whitespace passed.
These bounded checks are not a replacement for the final complete PR CI.

A final legacy-entry audit reproduced a third blocker using actual compiled
create/supersede/invalidate graphs: superseded v1 and invalidated v2 remained
raw ACTIVE and were incorrectly verified by the legacy report adapter. The
verifier now rejects evidence with a successor as well as explicitly inactive
evidence, without changing historical records. Four end-to-end cases cover
original/current values (verified) and superseded/invalidated values (blocked).
Before the fix the two withdrawn cases failed; after it the complete report
module passed 46 tests in 48.16 seconds. P0-10 advances to 0.4.7 for this behavior
change; current dispatch and historical 0.4.6 compatibility are synchronized.
Final installed/full-CI evidence is recorded on PR #99.

Long sessions remain subject to the documented registered-object capacity;
genuine continuous-browser/corrected-download acceptance and scientific
qualification remain explicitly unfinished. Final complete CI and merge
identities are recorded on [PR #99](https://github.com/starvingarc/BRIDGE/pull/99).
No existing service, private sample, locked evaluation or deployment was changed.

## Remaining scientific work

The 25 source-state cards, product-role/developmental-window decisions,
applicable program reviews and genuine biological-unit facts remain separate
requirements. Scope approval supplies none of them. The dependency order lives
in [Product Evidence Validation](../../plans/product-evidence-validation.md)
and [Cell-state scientific review](../../plans/p0-02-cell-state-scientific-freeze.md).

A genuine measured query → discriminating check → canonical graph update →
same-version portrait is still required once those inputs are applicable.
There is no scientific freeze, formal score, qualified report/export, clinical
efficacy/safety conclusion, product-release qualification, merge or deployment
established by this record.
