# P0-05 configurable off-target candidate validation — 2026-08-24

## Question

Can P0-05 deterministically report full-product role composition while keeping
all product roles and denominator decisions in a versioned, checksummed input?

This is engineering validation. Synthetic assignments do not validate a real
role map, OOD model, rare-state limit or product conclusion.

## Candidate interface

The candidate accepts ProductCase, ProductDefinitionCard, OffTargetRoleSpec,
CellStateEvidenceProfile and QCReadinessProfile through `ToolRequestV2`. It
publishes one OffTargetControlResult and no MeasurementResult or visualization.
`domain_score` is always null.

## Controls

- exact role, Schema, version, checksum, assay and cross-object binding;
- ProductDefinition, annotation-vocabulary and denominator-view binding;
- P0-02 reconciliation-state compatibility without denominator contamination;
- configurable target/adjacent/off-target/unresolved/unknown roles;
- missing sources, unmapped states and residual composition;
- strict numeric, deterministic reuse, input mutation and output-path tests;
- V1 typed refusal and public Schema/Pydantic state parity;
- source and installed-wheel execution.

## Evidence status

No local project code was run. The bounded closure implementation at
`92bd4278d7397cff985b51db0fa4cc864302fe6c` was transferred as a Git archive
to `/data1` and exercised there from both the exact source tree and a clean
wheel installation. The installed package resolved from the temporary
environment's `site-packages`, outside the source checkout. The wheel SHA-256
was `af81158efd23c9a0d09e77ce21ee9954b10a02c20de2dc8161b305ee9a2a6c33`.

| Gate | Current result |
|---|---|
| Source focused suite | 94 P0-03/P0-04/P0-05 tests passed |
| Source complete pytest | 1,053 passed; 3 existing dependency warnings |
| Installed-wheel complete pytest | 1,053 passed; 2 dependency warnings |
| 12-tool discovery | passed; exactly 12 |
| Knowledge validation | passed; no dangling method/source refs; 0 formal-eligible methods |
| Repository policy | passed |
| Schema/Card projection parity and diff check | passed |

The closure also removes P0-05's dependency on P0-03-private contract
implementations and rejects machine-local or credential-like configured and
upstream denominator text before any result or artifact is published. Product
roles, fractions, and OOD/rare-state boundaries are unchanged.

The PR remains Draft. These results demonstrate packaging and deterministic
mechanics, not approval of any product role or scientific release.

## Scientific boundary

P0-05 remains `candidate`. Static composition remains shadow; OOD and rare-state
calibration remain `not_assessed`. No zero observation becomes absence, harm,
safety, potency, release or ranking evidence.

## Peer-review closeout addendum

The combined closeout adds the exact ProductCase-bound
`BiologicalUnitManifest` and refuses manifest checksum/scope/member drift. The
change preserves experiment structure but does not alter StateRoleMap decisions
or make an OOD/rare-state conclusion. Final exact-head evidence is recorded in
the stack closeout validation.

The literature-driven re-review additionally separates an unassigned
composition residual from a known state whose product role is unresolved. A
denominator residual now enters `unknown` with
`composition_residual_identity_unknown`; P0-05 does not invent a technical or
biological cause. This preserves the full-product denominator without
misclassifying missing identity evidence as a reviewed cell-state role.
